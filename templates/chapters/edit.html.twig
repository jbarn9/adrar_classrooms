{% extends 'base.html.twig' %}

{% block title %}Editer le chapitre
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Editer le chapitre</h1>
		{{ include('chapters/_form.html.twig', {'button_label': 'Update'}) }}
		{{ include('chapters/_delete_form.html.twig') }}
		<div class="btn-edit">
			<div class="btn-actions">
				<div class="btn-return">
					<a href="{{ path('app_all_chapters') }}" class="btn btn-tertiary">Retour à la liste</a>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
